# 🇸🇪 CSS Flag of Sweden

A simple **HTML + CSS project** that recreates the **national flag of Sweden** using styled div elements. The flag features a blue background with a yellow Nordic cross, accurately positioned using absolute layout.

## ✨ Features
- **Accurate Colors**  
  - Blue → `#0269A6`  
  - Yellow → `#FECC00`  

- **Flag Layout**  
  - Blue rectangular background (`.flag-sweden`).  
  - Vertical yellow stripe (`.stripe-yellow`) positioned at 31.25% of the flag’s width.  
  - Horizontal yellow stripe (`.stripe-yellow2`) centered vertically across the flag.  

- **Customizable Variables**  
  - Colors and flag height defined in `:root` for easy adjustments.  
  - Width and stripe thickness can be modified for different proportions.  

- **Responsive Centering**  
  - Flexbox centers the flag both vertically and horizontally on the page.  

- **Minimal Structure**  
  - Simple HTML with two `.stripe` divs for the cross.  
  - Lightweight CSS for layout and coloring.  

## 🛠️ Tech Stack
- **HTML5**  
- **CSS3 (Flexbox + Positioning + Variables)**  

## 🚀 Use Cases
- Practicing CSS layout and positioning  
- Demonstrating how to build flags with pure CSS  
- Educational projects for learning absolute positioning  
- Creative UI elements for themed websites  

## 📂 Project Structure
```
index.html   # Main HTML structure
style.css    # Flag styling and layout
```
